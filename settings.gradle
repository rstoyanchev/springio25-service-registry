pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		mavenLocal()
		gradlePluginPortal()
	}
}
rootProject.name = 'springio25-service-registry'
